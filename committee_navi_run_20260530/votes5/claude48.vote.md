T2: YELLOW — E3 characterizes recompute COST by class but never runs the pointer-remap itself, so the repair primitive is still unmeasured as an intervention; worse, the free class (append, 0.90–0.96x) needs no repair while the only class that does (fixed interior, 1.3–2.4x) has exactly the unmeasured real-trace incidence you defer — so the runtime-primitive's payoff remains unvalidated end-to-end.
