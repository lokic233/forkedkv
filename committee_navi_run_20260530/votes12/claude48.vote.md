**T3: KILL** — E6 falsified the only differentiator (KV-divergence ≠ token-divergence): with the fp16 SDPA nondeterminism artifact controlled, token-prefix sharing *predicts* KV sharing, so RadixAttention's reactive approach already captures the entire opportunity. The lone survivor is the trivial 1−(tail/total) characterization both YELLOWs flagged — no non-trivial claim remains to defend, so retire it rather than launder the trivial form into a YELLOW.
