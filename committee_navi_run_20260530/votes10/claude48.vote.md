T2: GREEN — survives only as a correctly-scoped cost-map/negative-result (measured taxonomy + "activates iff harness interior-mutates") with the ~0% current-harness incidence reported up front; the conditional map is valid design guidance and does NOT depend on the unsupported "interior-mutating harnesses are coming" claim, so it stays green as long as we never imply broad active impact.
