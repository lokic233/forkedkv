67 skills discovered
T1: RED — The "schedulable resource" has no query/reserve API, so no scheduler can track K; the invariant is a CUDA-driver OOM artifact that collapses on ROCm/MI300X and is not a cross-vendor law. A microbench that predicts OOM is not a scheduler.
T2: RED — The RoPE-invariant subclass is vacuous for variable-length tool outputs; the 58µs VMM remap is an engine-specific memory hack that breaks correctness for dynamic RoPE and cannot be upstreamed. The 8.21× is a vLLM 0.6.6 implementation bug, not a fundamental pathology, and SGLang will not show >5×.
T3: YELLOW — The divergence model is a trivial 1 - (tail/total) description, not a predictive admission-control law; three coding harnesses do not establish a general "agentic" workload, and the 15% out-of-sample bar is easily overfit. No unfixable flaw, but the contribution is marginal characterization.
T1: RED — The "schedulable resource" has no query/reserve API, so no scheduler can track K; the invariant is a CUDA-driver OOM artifact that collapses on ROCm/MI300X and is not a cross-vendor law. A microbench that predicts OOM is not a scheduler.
T2: RED — The RoPE-invariant subclass is vacuous for variable-length tool outputs; the 58µs VMM remap is an engine-specific memory hack that breaks correctness for dynamic RoPE and cannot be upstreamed. The 8.21× is a vLLM 0.6.6 implementation bug, not a fundamental pathology, and SGLang will not show >5×.
T3: YELLOW — The divergence model is a trivial 1 - (tail/total) description, not a predictive admission-control law; three coding harnesses do not establish a general "agentic" workload, and the 15% out-of-sample bar is easily overfit. No unfixable flaw, but the contribution is marginal characterization.
